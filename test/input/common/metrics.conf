##################################################################
# Metrics
##################################################################
beam.metrics.level = "regular"

kamon.modules {
  prometheus-reporter {
    enabled = true
    name = "Beam Prometheus Reporter"
    description = "Exposes a scrape endpoint for Prometheus"
    factory = "beam.utils.metrics.BeamPrometheusReporter$Factory"
  }
}