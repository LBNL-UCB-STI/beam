include "new-york-PROD-baseline.conf"

beam.agentsim.simulationName = "NYC-prod-frequency-x2"

##################################################################
# BEAM ROUTING SERVICE
##################################################################
beam.routing {
  r5 {
    directory = ${beam.inputDirectory}"/r5-prod"
    osmFile = ${beam.routing.r5.directory}"/newyork-big-filtered.osm.pbf"
    osmMapdbFile = ${beam.routing.r5.directory}"/osm.mapdb"
  }
}
