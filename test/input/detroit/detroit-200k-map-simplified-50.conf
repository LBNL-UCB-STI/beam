include "../common/akka.conf"
include "../common/akka-router.conf"
include "../common/metrics.conf"
include "../common/matsim.conf"

include "detroit-200k.conf"

beam.agentsim.simulationName = "detroit-200k-map-simplified-50"

beam.agentsim.firstIteration = 0
beam.agentsim.lastIteration = 0

# population is 1,236,684 if during scenario loading  max radius is 100
# population is 3,821,278                             max radius is 10000
# population is 3,735,028                             max radius is 1000
beam.agentsim.agentSampleSizeAsFractionOfPopulation = 0.054


beam.agentsim.agents.vehicles.linkToGradePercentFilePath = ${beam.inputDirectory}"/r5-simplified-50/linkToGradePercent.csv"
beam.physsim.inputNetworkFilePath = ${beam.inputDirectory}"/r5-simplified-50/physsim-network.xml"
beam.routing {
  r5 {
    directory = ${beam.inputDirectory}"/r5-simplified-50"
    osmFile = ${beam.inputDirectory}"/r5-simplified-50/detroit-big-simplified_50.osm.pbf"
    osmMapdbFile = ${beam.inputDirectory}"/r5-simplified-50/osm.mapdb"
  }
}
