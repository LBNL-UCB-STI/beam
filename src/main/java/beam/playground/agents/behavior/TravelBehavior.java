package beam.playground.agents.behavior;

import beam.playground.agents.BeamAgent;

public class TravelBehavior extends BaseBehavior {

	public TravelBehavior(Boolean isActive, BeamAgent agent) {
		super(isActive, agent);
	}

}
