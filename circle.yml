machine:
  java:
    version: oraclejdk8
  environment:
    GRADLE_OPTS: '-Dorg.gradle.jvmargs="-Xmx512m -XX:+HeapDumpOnOutOfMemoryError"'

dependencies:
  pre:
    - git lfs pull
#  post:
#    - export TERM="dumb" if [ -e ./gradlew ]; then ./gradlew assemble;else gradle assemble;fi

test:
  timeout: 1800

notify:
  webhooks:
    - url: https://hooks.glip.com/webhook/d238705c-0001-4d98-81c1-3402db81911b
